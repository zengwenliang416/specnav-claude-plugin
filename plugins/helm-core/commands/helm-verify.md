---
description: Run Helm verification for the active OpenSpec change
argument-hint: "[optional test command]"
---

Load the `verify` skill and run the verifier.

If the argument is present, pass it as `HELM_TEST_COMMAND`.
