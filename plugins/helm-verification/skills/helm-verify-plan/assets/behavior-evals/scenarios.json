{
  "schema_version": 1,
  "scenarios": [
    {
      "id": "verify-runs-six-domains",
      "prompt": "/helm-verify",
      "expected": [
        "load active change",
        "generate verify plan",
        "run or block all six domains",
        "write aggregate report"
      ]
    }
  ]
}
